{
  "name": "anbe-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "client": "npm run dev --prefix client",
    "admin": "npm run dev --prefix admin",
    "worker": "npm run dev --prefix worker",
    "dev": "npm run client",
    "build": "npm install --prefix client && npm run build --prefix client",
    "install:all": "npm i --prefix client && npm i --prefix admin && npm i --prefix worker"
  }
}