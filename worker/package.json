{
    "name": "anbe-api",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "db:init": "wrangler d1 execute anbe-db --file=src/db/schema.sql",
        "db:init:remote": "wrangler d1 execute anbe-db --remote --file=src/db/schema.sql"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20240718.0",
        "typescript": "^5.5.0",
        "wrangler": "^3.65.0"
    }
}